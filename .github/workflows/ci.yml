name: CI de Testes do Backend

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test-backend:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v3

      # --- PASSO CORRIGIDO ---
      # Usa a action oficial do Docker que já prepara todo o ambiente,
      # incluindo o docker-compose (que agora é parte do comando 'docker compose')
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Instalar dependências
        run: npm install
        working-directory: backend

      # --- COMANDO 'docker-compose' ATUALIZADO ---
      - name: Iniciar contêineres Docker
        run: docker compose up -d
        working-directory: backend

      - name: Aguardar o banco de dados de teste
        run: sleep 15

      - name: Rodar Testes Unitários
        run: npm run test
        working-directory: backend

      - name: Rodar Testes de Integração
        run: npm run test:e2e
        working-directory: backend

      # --- COMANDO 'docker-compose' ATUALIZADO ---
      - name: Parar contêineres Docker
        if: always()
        run: docker compose down
        working-directory: backend